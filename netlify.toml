# Full-stack (SSR + server functions) deploy via the Nitro netlify preset.
# The preset writes .netlify/functions-internal + dist/client itself, so no
# public/_redirects SPA fallback is used.
[build]
  command = "DEPLOY_TARGET=netlify npm run build"

[build.environment]
  NODE_VERSION = "22"
